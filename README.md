# Project Site

## Stack
SvelteKit
Vercel (for deployments)

The app *currently* uses a single, hardcoded projects.ts to store project metadata and play links.

It uses /static/cover-images/ for images, and /static/play/[game-name] for game files. These URLs are hardcoded in projects.ts
